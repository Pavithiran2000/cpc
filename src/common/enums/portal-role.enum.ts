export enum PortalRole {
  Admin = 'ADMIN',
  Owner = 'OWNER',
}

export enum RecordStatus {
  Active = 'ACTIVE',
  Inactive = 'INACTIVE',
}

export enum ShiftStatus {
  Draft = 'DRAFT',
  Open = 'OPEN',
  Active = 'ACTIVE',
  Closing = 'CLOSING',
  Closed = 'CLOSED',
  Cancelled = 'CANCELLED',
}
